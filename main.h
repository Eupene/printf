#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

/* Const */
#define UNUSED(x) (void)(x)
#define BUFF_SIZE 1024


/* Props */
int _printf(const char *format, ...);
#endif /* MAIN_H */
